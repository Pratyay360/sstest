version: '3.8'

services:
  app:
    context: .
    container_name: ${REPO_NAME}
    dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    ports:
      - "5173:5173"
